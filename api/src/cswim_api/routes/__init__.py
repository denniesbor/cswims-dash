"""
Role: API routes package marker.
Author: Dennies Bor
Description:
    Each module in this package defines a FastAPI router for one resource
    (scenarios, satellites, vulnerability, etc). The main.py app file mounts
    each router under the /api prefix.
"""