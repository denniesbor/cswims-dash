# Production overrides for Proxmox.
# All port values come from .env.

services:
  api:
    command: ["uvicorn", "cswim_api.main:app", "--host", "0.0.0.0", "--port", "8000"]
    environment:
      ENVIRONMENT: production
